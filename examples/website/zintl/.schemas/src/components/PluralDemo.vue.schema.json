{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "Fewer": {
      "type": "string"
    },
    "More": {
      "type": "string"
    },
    "Showing {count} of 12 files": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        }
      ],
      "description": "Variables: {count}"
    },
    "Six forms, because Arabic has six. Your source still says one sentence.": {
      "type": "string"
    },
    "Switch the language in the bar at the top and press the numbers again. The grammar changes; the line in your source does not. And none of it reaches the browser as a rule to interpret — the forms compile to plain JavaScript at build time.": {
      "type": "string"
    },
    "This language needs no plural forms for this sentence, so its catalog carries none.": {
      "type": "string"
    },
    "This language's catalog": {
      "type": "string"
    },
    "Two forms, because that is what this language distinguishes.": {
      "type": "string"
    },
    "Your source": {
      "type": "string"
    }
  },
  "required": [
    "Fewer",
    "More",
    "Showing {count} of 12 files",
    "Six forms, because Arabic has six. Your source still says one sentence.",
    "Switch the language in the bar at the top and press the numbers again. The grammar changes; the line in your source does not. And none of it reaches the browser as a rule to interpret — the forms compile to plain JavaScript at build time.",
    "This language needs no plural forms for this sentence, so its catalog carries none.",
    "This language's catalog",
    "Two forms, because that is what this language distinguishes.",
    "Your source"
  ],
  "additionalProperties": false
}

import { _t } from "virtual:zintl/runtime/internal";
import _zintl_mgr_b_src_index_render from "virtual:zintl/manager/none/entry:b_src_index_render";
/**
 * The strings reach Zintl because they are assigned to `innerHTML`.
 *
 * That is not incidental. Extraction stitches *HTML* out of template literals,
 * and it knows a template literal is HTML from where it is written to — an
 * assignment sink like `innerHTML`. A bare `return \`<h1>…\`` is just a string
 * as far as the extractor can tell, and is left alone. Same rule on both hosts.
 */
export function Home(): HTMLElement {
  const container = document.createElement("div");
  container.className = "content";

  container.innerHTML = `
    <h1>${_t("Vanilla Rsbuild", { _mgr: _zintl_mgr_b_src_index_render, _bId: "b_src_pages_Home_Home" })}</h1>
    <p>${_t("Start building amazing things with Rsbuild.", { _mgr: _zintl_mgr_b_src_index_render, _bId: "b_src_pages_Home_Home" })}</p>
    <p>${_t("Edit <code>src/pages/Home.ts</code> and save to test <code>HMR</code>", { _mgr: _zintl_mgr_b_src_index_render, _bId: "b_src_pages_Home_Home", _tags: [{"alias":"code","originalOpen":"<code>","tagName":"code"}] })}</p>
    <nav class="nav"><a id="to-about" href="/about" data-link>${_t("Read the guide", { _mgr: _zintl_mgr_b_src_index_render, _bId: "b_src_pages_Home_Home" })}</a></nav>
  `;

  return container;
}
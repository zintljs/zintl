import { _t, loadI18nInstance, subscribe, getStoreVersion } from "virtual:zintl/runtime/internal";
import { createSignal as __zintl_createSignal } from "solid-js";
const [__zintl_v, __zintl_set_v] = __zintl_createSignal(getStoreVersion());
subscribe(() => __zintl_set_v(getStoreVersion()));
import _zintl_mgr_b_f1bc61690f0b from "virtual:zintl/manager/none/entry:b_f1bc61690f0b";
import { createSignal } from "solid-js";

import { LocaleBar } from "./locale-bar.tsx";

export function App() {
  const [lang, setLang] = createSignal(
    new URLSearchParams(window.location.search).get("lang") || "en",
  );

  const onSwitch = async (next: string) => {
    const url = new URL(window.location.href);
    url.searchParams.set("lang", next);
    window.history.pushState({}, "", url.pathname + url.search);
    await (globalThis.__zintl_inst = loadI18nInstance({ locale: next, loaders: { ["b_f1bc61690f0b"]: _zintl_mgr_b_f1bc61690f0b.loader } }));
    setLang(next);
  };

  /* No remount wrapper, deliberately: if the reactive bridge works these
     strings update in place, and if it does not this fixture fails —
     which is the whole reason it exists. */
  return (
    <div>
      <LocaleBar lang={lang()} onSwitch={onSwitch} />
      <h1>{_t("90e40d50", { _mgr: _zintl_mgr_b_f1bc61690f0b, _bId: "b_2f98939c81ca", _v: __zintl_v() })}</h1>
      <p>{<span style={{ display: "contents" }} innerHTML={_t("b5323d23", { _mgr: _zintl_mgr_b_f1bc61690f0b, _bId: "b_2f98939c81ca", _tags: [{ alias: "code", originalOpen: `<code>`, tagName: "code" }, { alias: "code", originalOpen: `<code>`, tagName: "code" }], _v: __zintl_v() })} />}</p>
    </div>
  );
}